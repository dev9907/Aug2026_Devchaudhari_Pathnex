Day 05 — Advanced CI/CD and Infrastructure Provisioning

🔹 Jenkinsfile — With Git Integration and Testing


```groovy
pipeline{
     agent any
     stage {
         satege('Clone Repository") {
              steps {
                  git 'https://github.com/pathnex/sample-repo.git'
               }
           }
           stage('Build') {
              steps {
                 echo 'Building project from Git Reopsitory'
              }
           }
           stage('Test') {
                steps {
                    echo 'Running unit tests'
                }
           }
           stage('Deploy')
               steps {
                    echo 'Deploying application'
                }
            }
       }
 }

GitLab CI/CD - With Docker Build
stages:
 - build
 - push
 - deploy

build:
  stage: build
  script:
    - docker build -t pathnex-app .

push:
   stage: push
script:
 - docker push pathnex-app

deploy:
   stage: deploy
   script:
   -kubect1 apply -f kubernets.deployment.yaml
  Ansible - Create User and Ser Permissoins
-name: Create a new user
 hosts: all
 become: yes
 task:
     - name: Create user
      user:
        name:pathnex
        state: present
        shell: /bin/bash
     -name: Set permission for the user
      file:
        path: /home/pathnex
        owner: pathnex
        group: pathnex
        mode: '0755'
     Terraform - EC2 with Tages and Volume
resource "aws-intance" "PathnexEC2"{
    ami         = "ami-0abcd1234abcd1234"
    intance_type =t2.medium"

    tags = {
      name = "pathnex-Server"
    }

   ebs_block_device {
     device_name = "/dev/sdh"
     volume_size = 50
   }
}


  Docker
# Python Application

# Docker FIle
From python:3.11
WORKDIR /opt/pthnex/python-app
COPY app.py /opt/pathnex/python-app
CMD ["PYTHON","/opt/pahnex/python-app/app/py"]

# Real Path
/opt/pahnex/python-app

